# crossword-challenge-platform-14-24

This workspace contains the React frontend for the Crossword Challenge platform.

- Frontend: `frontend_react` (React 18, react-router)
- Backend: Django REST API (integrated via environment-based base URL)

See `frontend_react/README.md` for full details.